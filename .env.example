# Google OAuth credentials — from https://console.cloud.google.com/apis/credentials
GOOGLE_CLIENT_ID=
GOOGLE_CLIENT_SECRET=
# Authorized redirect URI (must match exactly what's set in Google Cloud Console)
GOOGLE_REDIRECT_URI=http://localhost:3000/auth/google/callback

DATABASE_URL=postgres://localhost:5432/backend_e2e_test
PORT=3000

# JWT signing secret (use a long random string in production)
JWT_SECRET=
JWT_EXPIRES_IN=7d
