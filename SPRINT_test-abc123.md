# Sprint test-abc123 — Summary

test

**Tasks**: 1 done / 0 failed / 0 blocked

## Done

### t1 — endpoint GET /google

```
Both tests pass. Added `GET /google` to `src/app.js` returning `{ url: 'https://www.google.com' }` and a corresponding test in `tests/google.test.js`.
```

