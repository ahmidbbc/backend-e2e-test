# Sprint sprint-c7e60f2a — Summary

Noté.

**Tasks**: 1 done / 0 failed / 0 blocked

## Done

### task-1 — Ajouter l'endpoint GET /palindrome

```
Done. Added the `GET /palindrome` endpoint in src/app.js:129 and a test file — all 4 tests pass.

The endpoint reads the `mot` query param, compares it against its reverse by code point (so accents/emoji work correctly), and returns `{ mot, isPalindrome }`. Missing input is treated as an empty string (a palindrome).
```

